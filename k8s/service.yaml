apiVersion: v1
kind: Service
metadata:
  name: bank-sampah-service
  labels:
    app.kubernetes.io/name: bank-sampah-app
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: bank-sampah-app
