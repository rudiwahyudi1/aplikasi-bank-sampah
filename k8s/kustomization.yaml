apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - configmap.yaml
  - deployment.yaml
  - service.yaml

commonLabels:
  environment: production
  managed-by: devsecops-ci-cd
